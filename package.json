{
  "name": "workshop-demo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ gcbsumid/workshop-demo",
    "create-local": "graph create --node http://localhost:8020/ gcbsumid/workshop-demo",
    "remove-local": "graph remove --node http://localhost:8020/ gcbsumid/workshop-demo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gcbsumid/workshop-demo"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
